{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Mermaid Watch",
        "type": "shell",
        "command": "mmdc",
        "args": [
          "-i", "${file}",
          "-o", "${fileDirname}/${fileBasenameNoExtension}.svg",
          "-b", "transparent"
        ],
        "group": "build",
        "presentation": {
          "reveal": "silent",
          "panel": "dedicated"
        },
        "problemMatcher": []
      }
    ]
}
